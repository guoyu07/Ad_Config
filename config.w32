// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yaconf", "for yaconf support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yaconf", "enable yaconf support", "no");

if (PHP_YACONF != "no") {
	EXTENSION("yaconf", "yaconf.c");
}

