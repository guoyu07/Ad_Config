// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ad_config", "for ad_config support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ad_config", "enable ad_config support", "no");

if (PHP_YACONF != "no") {
	EXTENSION("ad_config", "ad_config.c");
}

